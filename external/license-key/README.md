# License-Key SDK boundary

The `machineId.js` file under this directory is a copied third-party runtime file and is read-only. The current pilot uses it only to derive `machine_id`; local software-license verification is not included.

Dongil/OCR integration code must live outside this directory. Do not edit these files to change application behavior; update from the approved License-Key source and verify the integration adapter instead.
